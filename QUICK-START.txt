╔═══════════════════════════════════════════════════════════════════════════════╗
║                   🔧 NGINX + PM2 PRODUCTION FIX - QUICK START                 ║
╚═══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ ✅ WHAT WAS FIXED                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│ 1. Backend now binds to 0.0.0.0 in production (Nginx can connect)          │
│ 2. Added /api endpoint listing all routes                                   │
│ 3. Added 404 handler for undefined /api/* routes                           │
│ 4. Enhanced CORS to support production domain                              │
│ 5. Created complete Nginx configuration                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📋 IMMEDIATE ACTIONS (Production Server)                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│ 1. Add to backend/.env:                                                     │
│    PRODUCTION_DOMAIN=your-domain.com                                        │
│                                                                              │
│ 2. Deploy Nginx config:                                                     │
│    sudo cp nginx.conf /etc/nginx/sites-available/property-management       │
│    sudo nano /etc/nginx/sites-available/property-management                │
│    # Update: server_name your-domain.com                                   │
│    sudo ln -s /etc/nginx/sites-available/property-management \             │
│              /etc/nginx/sites-enabled/                                      │
│    sudo nginx -t                                                            │
│    sudo systemctl reload nginx                                              │
│                                                                              │
│ 3. Restart backend:                                                         │
│    pm2 restart property-backend                                             │
│    pm2 logs property-backend                                                │
│                                                                              │
│ 4. Verify:                                                                  │
│    curl http://localhost:4002/api                                           │
│    curl http://your-domain.com/api                                          │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🧪 VERIFICATION TESTS                                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│ Direct backend test:                                                        │
│ $ curl http://localhost:4002/api                                            │
│ ✅ Should return: {"success":true,"message":"Property Management API",...}  │
│                                                                              │
│ Through Nginx test:                                                         │
│ $ curl http://your-domain.com/api                                           │
│ ✅ Should return: Same JSON as above                                        │
│                                                                              │
│ 404 test:                                                                   │
│ $ curl http://your-domain.com/api/nonexistent                               │
│ ✅ Should return: {"success":false,"error":"API endpoint not found",...}    │
│                                                                              │
│ Frontend test:                                                              │
│ $ curl http://your-domain.com/                                              │
│ ✅ Should return: HTML content                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🐛 TROUBLESHOOTING                                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│ 502 Bad Gateway:                                                            │
│   → Check: pm2 status                                                       │
│   → Check: sudo netstat -tulpn | grep 4002                                  │
│   → Fix: pm2 restart property-backend                                       │
│                                                                              │
│ Cannot GET /api:                                                            │
│   → Check: sudo nginx -t                                                    │
│   → Check: curl http://localhost:4002/api (test direct)                    │
│   → Fix: sudo systemctl reload nginx                                        │
│                                                                              │
│ CORS errors:                                                                │
│   → Check: PRODUCTION_DOMAIN in .env                                        │
│   → Fix: Add domain to .env, restart PM2                                   │
│                                                                              │
│ File uploads fail:                                                          │
│   → Check: client_max_body_size 10M; in nginx.conf                         │
│   → Check: sudo ls -la backend/uploads                                      │
│   → Fix: sudo chown -R www-data:www-data backend/uploads                   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📊 STATUS CHECKS                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ Backend:  pm2 status                                                        │
│ Logs:     pm2 logs property-backend --lines 50                              │
│ Monitor:  pm2 monit                                                         │
│                                                                              │
│ Nginx:    sudo systemctl status nginx                                       │
│ Config:   sudo nginx -t                                                     │
│ Logs:     sudo tail -f /var/log/nginx/property-management-error.log        │
│                                                                              │
│ Port:     sudo netstat -tulpn | grep 4002                                   │
│ Process:  ps aux | grep node                                                │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION FILES                                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│ • DEPLOYMENT.md  - Complete step-by-step deployment guide                  │
│ • CHANGES.md     - Detailed summary of all changes made                    │
│ • nginx.conf     - Production Nginx configuration template                 │
│ • This file      - Quick reference card                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ ⚠️  CRITICAL CONFIGURATION NOTES                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│ 1. Backend MUST bind to 0.0.0.0 in production (automatic when              │
│    NODE_ENV=production)                                                     │
│                                                                              │
│ 2. All API routes MUST be under /api prefix                                │
│                                                                              │
│ 3. Nginx proxy_pass MUST be: http://backend_api;                           │
│    (NO trailing slash after backend_api)                                    │
│                                                                              │
│ 4. Production domain MUST be added to .env as PRODUCTION_DOMAIN            │
│                                                                              │
│ 5. The /api/* 404 handler MUST come AFTER all route definitions            │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🎯 FINAL CHECKLIST                                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│ [ ] Added PRODUCTION_DOMAIN to backend/.env                                │
│ [ ] Deployed nginx.conf to /etc/nginx/sites-available/                     │
│ [ ] Updated server_name in nginx config                                    │
│ [ ] Created symbolic link in sites-enabled/                                │
│ [ ] Tested nginx config (nginx -t)                                         │
│ [ ] Reloaded nginx (systemctl reload nginx)                                │
│ [ ] Restarted backend (pm2 restart property-backend)                       │
│ [ ] Verified curl http://localhost:4002/api works                          │
│ [ ] Verified curl http://your-domain.com/api works                         │
│ [ ] Tested frontend loads in browser                                       │
│ [ ] Tested API calls from frontend work                                    │
│ [ ] No CORS errors in browser console                                      │
└─────────────────────────────────────────────────────────────────────────────┘

Need help? See DEPLOYMENT.md for detailed instructions and troubleshooting.
