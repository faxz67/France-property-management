# ===================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# Server: localhost
# ===================================
# INSTRUCTIONS: Copy this file to .env on your server and update the values
# cp env.production.template .env
# nano .env (then update the CHANGE_THIS values)
# ===================================

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=property_management
DB_USER=property_user
DB_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# JWT Configuration
JWT_SECRET=GENERATE_RANDOM_SECURE_JWT_KEY_HERE_MIN_32_CHARS
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=4002
NODE_ENV=production
FRONTEND_URL=http://localhost:5174
BACKEND_ORIGIN=http://localhost:4002

# Rate Limiting - Optimized for Multi-User Support
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=2000

# Session Configuration
SESSION_SECRET=GENERATE_RANDOM_SECURE_SESSION_KEY_HERE_MIN_32_CHARS

# Database Connection Pool (Multi-User Optimization)
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_POOL_ACQUIRE=60000
DB_POOL_IDLE=30000

# Server Timeouts (Multi-User Support)
SERVER_KEEP_ALIVE_TIMEOUT=65000
SERVER_HEADERS_TIMEOUT=66000

# Email Configuration (for sending receipts)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
EMAIL_USER=propertymanagement534@gmail.com
EMAIL_PASS=kcmi ueai msmg rtyo

# File Upload Configuration
UPLOAD_DIR=public/uploads
MAX_FILE_SIZE=10485760

# ===================================
# IMPORTANT SECURITY NOTES:
# ===================================
# 1. Change DB_PASSWORD to a strong password
# 2. Generate a random JWT_SECRET (minimum 32 characters)
# 3. Generate a random SESSION_SECRET (minimum 32 characters)
# 4. Update EMAIL_USER and EMAIL_PASS with your SMTP credentials
#
# To generate secure keys on the server, run:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ===================================

